# URL del backend API
# Por defecto: http://localhost:8000
# Si tu backend corre en otro puerto (ej: 8001), cambia el puerto aquí
# Si usas ngrok o un servidor remoto, usa la URL completa
REACT_APP_API_URL=http://localhost:8001
